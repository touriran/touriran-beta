extends _default

block vars

block default
  section.uk-section.uk-section-small
    div.uk-container
      div.uk-grid-medium.uk-child-width-1-1(uk-grid)

        div.uk-text-center
          if pageData.breadcrumbs
            ul.uk-breadcrumb.uk-flex-center.uk-margin-remove
              li: a(href="index.html") Home
              each href, breadcrumb in pageData.breadcrumbs
                li: a(href= href)= breadcrumb
              if pageData.title
                li: span= pageData.title
          if pageData.title
            h1.uk-margin-small-top.uk-margin-remove-bottom= pageData.title
          if page.quantity
            div.uk-text-meta.uk-margin-xsmall-top #{page.quantity} items

        div
          block pages